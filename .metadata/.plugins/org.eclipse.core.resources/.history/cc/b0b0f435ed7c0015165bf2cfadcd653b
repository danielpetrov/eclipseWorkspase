<?php
require_once 'Readline.php';

$a = readline ( "Enter a:" . PHP_EOL );

$b = readline ( "Enter b:" . PHP_EOL );

var_dump ( $a );
var_dump ( $b );

if (! (is_numeric ( $a ) && is_numeric ( $b ))) {
	throw new Exception ( "Please enter numeric value for a, b!" . PHP_EOL );
}

if (! (floor ( $a ) == $a) && floor ( $b ) == $b) {
	throw new Exception ( "Please enter numeric value for a, b!" . PHP_EOL );
}